# LiveKit voice agent dependencies (separate from capture API)
# Install into /home/lordmuffin/.agent-venv

# LiveKit agents framework + plugins
livekit-agents[openai,silero]>=1.0.0

# Faster-whisper for local STT (optional — can also proxy through LiteLLM)
faster-whisper>=1.0.0

# Used by vault_tools.py (stdlib only — no extra deps needed)
