<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required on Android 13+ to show download progress notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- External storage for persistent data (history, logs) that survives uninstall.
         MANAGE_EXTERNAL_STORAGE is the API 30+ path (user grants via All Files Access).
         WRITE_EXTERNAL_STORAGE + requestLegacyExternalStorage covers API 29 and below. -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />
    <!-- Required for startBluetoothSco() to route audio to Bluetooth headset mic -->
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:name=".JarvisApp"
        android:allowBackup="false"
        android:requestLegacyExternalStorage="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.JarvisVoice"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- System libraries required for hardware acceleration backends.
             Android 12+ (API 32+) restricts loading non-SDK native libraries; apps must
             declare each one with uses-native-library (required="false" so the app still
             installs on devices that don't have the library, e.g. non-Pixel, non-Qualcomm).
             uses-native-library must be a child of <application>, not <manifest>.
             libedgetpu_litert.so  — Google Tensor NPU dispatch (Pixel 6+ / Tensor G1+)
             libOpenCL.so          — OpenCL for GPU backend (most modern Android devices)
             libcdsprpc.so         — Qualcomm DSP RPC for Snapdragon NPU (non-Pixel devices) -->
        <uses-native-library android:name="libedgetpu_litert.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL.so" android:required="false"/>
        <uses-native-library android:name="libcdsprpc.so" android:required="false"/>

        <activity
            android:name=".SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="@string/app_name" />

        <activity
            android:name=".HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="Dictation History" />

        <activity
            android:name=".DictionaryActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="Custom Dictionary" />

        <activity
            android:name=".DebugLogActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="Debug Log" />

        <activity
            android:name=".ModelManagerActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="LLM Model" />

        <activity
            android:name=".SttModelManagerActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="STT Model" />

        <activity
            android:name=".VoiceChatActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="Kai Chat" />

        <activity
            android:name=".VaultCaptureActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="Voice to Vault" />

        <activity
            android:name=".AgentTaskActivity"
            android:exported="false"
            android:theme="@style/Theme.JarvisVoice"
            android:label="Agent Tasks" />

        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:theme="@style/Theme.JarvisVoice">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.JarvisVoice.Transparent" />

        <service
            android:name=".VoiceOverlayService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <service
            android:name=".JarvisAccessibilityService"
            android:exported="true"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- WorkManager's SystemForegroundService needs foregroundServiceType declared on
             Android 14+ or setForeground(FOREGROUND_SERVICE_TYPE_DATA_SYNC) throws. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="shortService|dataSync"
            tools:node="merge" />

    </application>
</manifest>
