<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/jv_bg">

    <!-- Task list — leaves room for input panel + bottom nav -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_tasks"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="200dp"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="8dp" />

    <!-- Empty state (centered in upper portion) -->
    <LinearLayout
        android:id="@+id/ll_empty"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="200dp"
        android:orientation="vertical"
        android:gravity="center"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="🤖"
            android:textSize="48sp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="No agent tasks yet"
            android:textColor="@color/jv_text"
            android:textSize="16sp"
            android:layout_marginTop="12dp" />

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Type a prompt below and tap Run"
            android:textColor="@color/jv_text2"
            android:textSize="13sp"
            android:layout_marginTop="4dp" />

    </LinearLayout>

    <!-- Input panel pinned above bottom nav -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:layout_marginBottom="64dp"
        android:orientation="vertical"
        android:background="@color/jv_bg"
        android:paddingTop="8dp">

        <!-- Model selector + task name row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingBottom="4dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Model:"
                android:textColor="@color/jv_text2"
                android:textSize="12sp"
                android:layout_marginEnd="6dp" />

            <Spinner
                android:id="@+id/sp_model"
                android:layout_width="0dp"
                android:layout_height="36dp"
                android:layout_weight="1"
                android:background="@drawable/pill_background_dim"
                android:paddingStart="10dp"
                android:paddingEnd="4dp" />

            <EditText
                android:id="@+id/et_task_name"
                android:layout_width="0dp"
                android:layout_height="36dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:background="@drawable/pill_background_dim"
                android:hint="Name (optional)"
                android:textColorHint="@color/jv_text2"
                android:textColor="@color/jv_text"
                android:textSize="12sp"
                android:paddingStart="10dp"
                android:paddingEnd="10dp"
                android:inputType="textCapSentences"
                android:maxLines="1"
                android:singleLine="true" />

        </LinearLayout>

        <!-- Prompt input + Run button -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="bottom"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingBottom="6dp">

            <EditText
                android:id="@+id/et_prompt"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:minLines="2"
                android:maxLines="5"
                android:background="@drawable/pill_background_dim"
                android:hint="What should Kai work on?"
                android:textColorHint="@color/jv_text2"
                android:textColor="@color/jv_text"
                android:textSize="14sp"
                android:paddingStart="14dp"
                android:paddingEnd="14dp"
                android:paddingTop="10dp"
                android:paddingBottom="10dp"
                android:inputType="textMultiLine|textCapSentences"
                android:gravity="top"
                android:layout_marginEnd="8dp" />

            <Button
                android:id="@+id/btn_run_task"
                android:layout_width="64dp"
                android:layout_height="52dp"
                android:text="▶ Run"
                android:textSize="11sp"
                android:textAllCaps="false"
                android:background="@drawable/pill_background_accent"
                android:textColor="@color/jv_bg"
                android:stateListAnimator="@null"
                android:layout_gravity="bottom" />

        </LinearLayout>

    </LinearLayout>

    <include
        layout="@layout/bottom_nav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom" />

</FrameLayout>
